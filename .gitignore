# Local archive of pre-launch reference files (kept on disk, not deployed).
_archive/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
*.swp
.vscode/
.idea/

# Build/runtime artefacts
.cache/
node_modules/

# Local Claude Code dev tooling (preview server config)
.claude/

# Portrait pipeline working files (45 MB of FAL.AI variants, cutouts, scripts).
# The final output ships as assets/portrait.jpg — source artefacts stay local.
assets/portrait-source/

# Local-only preview/scratchpad files. Used as source-of-truth during in-progress
# reorgs (e.g. the four-lane homepage restructure); intentionally never deployed.
homepage-preview-*.html
